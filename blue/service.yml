apiVersion: v1
kind: Service
metadata:
  namespace: sandbox
  name: blue-service
spec:
  type: NodePort
  selector:
    app: blue
  ports:
    # no "https" is defined
    - name: http
      port: 80
      targetPort: 8081
